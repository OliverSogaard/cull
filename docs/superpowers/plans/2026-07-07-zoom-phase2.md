# Zoom Phase 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline). Approved by Oliver 2026-07-07 ("the two others you can go ahead on now"); distribution stays parked.

**Goal:** Complete the zoom modernization: (1) prefetch the next frame's zoom full while zoomed, (2) cursor-anchored mouse zoom in loupe, (3) rate-while-zoomed carry in compare mode.

**Architecture:** All three ride the shipped carry machinery (`keepZoomOnAdvanceRef`, `zoomSwapInstant`/`zoomGlide`). Mouse zoom mirrors Space exactly (hold-based: press = zoom at cursor, drag = pan, release = exit). Compare carry needs the same transform-safe measure fix CompareView-side that 069a06c gave the loupe.

**Tech stack/gates:** React 19 + TS (vitest), no Rust changes. `pnpm test` + `pnpm tsc --noEmit` green per task; commit per task; code-review at the end; Oliver live-gates visuals.

## Global constraints
- No em-dashes in user-facing strings. Zoom stays hold-based (no sticky zoom state, no wheel-zoom).
- Keyboard pan keeps PAN_LIMIT (±40%); mouse anchor/drag clamps by origin bounds [0,100] instead (a corner anchor legitimately exceeds 40).
- NAS-safe: prefetch only after the current frame's full landed (never competes with the active zoom fetch).

### Task 1: Prefetch next full while zoomed
**Files:** Modify `src/App.tsx` (new effect near the hiRes settle effect).
- [ ] Effect: when `phase==="culling" && !compareMode && isZooming && cur.stage === "full"`, compute `nextTarget = visibleIndices[positionInFilter+1]`, call `imageStore.requestZoomFull(nextPath)` once per (currentIndex, condition) — guard with a ref so it fires once per frame parked.
- [ ] Verify store semantics: requestZoomFull is idempotent/cached; fullKeep window (±2-3) retains the neighbor.
- [ ] Commit `feat: zoomed frame prefetches the next frame's full (carried advance lands sharp)`.

### Task 2: Cursor-anchored zoom (loupe)
**Files:** Modify `src/App.tsx` (mouse handlers + state), `src/App.css` (cursors), `src/components/HelpOverlay.tsx`.
- [ ] `onMouseDown` (left, loupe stage only, not while already zoomed): cursor % within `imgRect` → `setPanOffset({x: cursorPctX - afX, y: cursorPctY - afY})` (origin = cursor), `setIsZooming(true)`, `setZoomLevel(e.shiftKey ? 2 : 1)`, `mouseZoomRef.current = true`.
- [ ] Window `mousemove` while mouse-zoom held: drag-pan — `dPct = -dx / imgRect.width * 100 / (zoomZ - 1)` per axis (grab semantics), clamp pan so origin stays [0,100].
- [ ] Window `mouseup` (+ blur): if `mouseZoomRef` → resetZoom, clear ref. Space keyup keeps its unconditional reset (tapping Space mid-mouse-zoom exits: accepted).
- [ ] Rating while mouse-zoomed carries (already generic via isZooming).
- [ ] CSS: `cursor: zoom-in` on the loupe photo area; `grabbing` while mouse-zoomed.
- [ ] HelpOverlay loupe navigate: `["click (hold)", "zoom at cursor · drag to pan"]`.
- [ ] Commit `feat: cursor-anchored zoom (hold click at the point, drag pans)`.

### Task 3: Compare rate-while-zoomed
**Files:** Modify `src/App.tsx` (compare decide keys + carry), `src/components/CompareView.tsx` (zoomGlide prop + transform-safe measure while zoomed).
- [ ] Read CompareView's measure machinery; apply the 069a06c parent-clip transform-safe measure for zoomed re-measures (both panes).
- [ ] Thread `zoomGlide` prop into CompareView (replace internal `zoomTransition(isZooming)` uses).
- [ ] Unblock Enter/Backspace/k/f while zoomed in compare keymap; in `challengerWins` set `zoomSwapInstant` + pan reset when zoomed (champion changes → new AF origin must land instantly); in `challengerLoses`/`challengerKeptBoth` keep pan (champion view must not jump), set `zoomSwapInstant` when zoomed (challenger pane content swaps).
- [ ] Auto-exit paths (`goBack`) already resetZoom.
- [ ] Commit `feat: compare decides while zoomed (carry across challenger changes)`.

### Final gate
- [ ] Suites green, code-review agent over the three commits, fix findings, report. Oliver live-gates; push on say-so.
