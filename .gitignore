# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node / Vite
node_modules
dist
dist-ssr
.vite
*.local

# Rust / Tauri build output
src-tauri/target

# Local test fixtures (real CR3s, not for the repo)
sample_cr3s

# Python bytecode cache (scripts/ dir)
scripts/__pycache__/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code session metadata
.claude

# Ad-hoc screenshots and dropped source art at repo root — working files,
# not repo content. Prepared/derived assets live under src/assets/.
/Screenshot *.png
/isolated-desert-landscape-with-cactus.png

# Large ML models kept out of git — published as release assets on models-v1,
# pulled (sha256-pinned) by scripts/fetch-models.sh; installers bundle them.
src-tauri/models/clip_vitb32_visual.onnx
src-tauri/models/dinov2s.onnx
